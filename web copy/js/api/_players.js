/*
 * DO NOT EDIT THIS FILE, it is auto-generated. It will be updated automatically.
 * All changes done to this file will be lost upon re-running the 'silence createapi' command.
 * If you want to create new API methods, define them in a new file.
 *
 * Silence is built and maintained by the DEAL research group at the University of Seville.
 * You can find us at https://deal.us.es
 */

"use strict";

import { BASE_URL, requestOptions } from './common.js';

const playersAPI_auto = {

    /** Gets all entries from 'players' */
    getAll: async function() {
        let response = await axios.get(`${BASE_URL}/players`, requestOptions);
        return response.data;
    },

    /** Gets an entry from 'players' by its primary key */
    getById: async function(peopleId) {
        let response = await axios.get(`${BASE_URL}/players/${peopleId}`, requestOptions);
        return response.data[0];
    },

    /** Creates a new entry in 'players' */
    create: async function(formData) {
        let response = await axios.post(`${BASE_URL}/players`, formData, requestOptions);
        return response.data;
    },

    /** Updates an existing entry in 'players' by its primary key */
    update: async function(formData, peopleId) {
        let response = await axios.put(`${BASE_URL}/players/${peopleId}`, formData, requestOptions);
        return response.data;
    },

    /** Deletes an existing entry in 'players' by its primary key */
    delete: async function(peopleId) {
        let response = await axios.delete(`${BASE_URL}/players/${peopleId}`, requestOptions);
        return response.data;
    },
};

export { playersAPI_auto };